import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  label: {
    fontWeight: '600',
    fontSize: 15,
    marginBottom: 5,
    marginLeft: 1,
  },
});

export default styles;
